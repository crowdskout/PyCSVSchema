{
    "fields": [
        {
            "name": "id",
            "type": "string",
            "format": "",  // only for string
            "pattern": "reg",  // only for string
            "groupChar": "",  // only for number and int
            "trueValues": ["true", "True", "TRUE", "1"], // only for boolean
            "falseValues": ["false", "False", "FALSE", "0"], // only for boolean
            "minLength": 1,  // only for string
            "maxLength": 10,  // only for string
            "multipleOf": false,  // only for number and int
            "minimum": 10,  // only for number and int
            "exclusiveMinimum": false,  // only for number and int
            "maximum": 100,  // only for number and int
            "exclusiveMaximum": false,  // only for number and int
            "enum": [1,2,3],
            "required": false,
            "nullable": true
        }
    ],
    "missingValues": ["NA"],
    "definitions": {
        "id": {
            "type": "integer"
        }
    },
    "exactFields": false,
    "dependencies": {
        "state_voter_id": ["state"]
    },
    "patternFields": {
        "^S_": {
            "type": "string"
        },
        "^I_": {
            "type": "integer"
        }
    },
    "additionalFields": true
}